<style>
      body {
            /* background-color: #000; */
            color: #fff;
            font-family: Arial, sans-serif;
           
        }
        .reset-main{
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .reset-container {
          
            background-color: black;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 0 10px black;
            max-width: 400px;
            width: 100%;
            text-align: center;
        }

        .reset-container h1 {
            font-size: 24px;
            margin-bottom: 20px;
        }

        .reset-container p {
            font-size: 16px;
            margin-bottom: 20px;
        }

        .form-control {
            width: 100%;
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #000;
            border-radius: 5px;
            background-color: #ffd1d1;
            color: #000;
        }

        .form-control:focus {
            border-color: #fff;
            outline: none;
        }

        .submit-btn {
            width: 100%;
            padding: 10px;
            border: none;
            border-radius: 5px;
            background-color: #fff;
            color: #000;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s, color 0.3s;
        }

        .submit-btn:hover {
            background-color: #555;
            color: #fff;
        }

        @media (max-width: 480px) {
            .reset-container {
                padding: 15px;
            }

            .reset-container h1 {
                font-size: 20px;
            }

            .reset-container p {
                font-size: 14px;
            }

            .submit-btn {
                font-size: 14px;
            }
        }
        .enter-email{
            
            margin-right: 40px;
          
        }
</style>
<%- include('navbar') %>

<!-------------------------------------------- Forget Password ------------------------------------------------->

<main class="reset-main">
    <div class="reset-container">
        <h1>Reset Your Password</h1>
        <p>Don’t worry. It happens. Please enter the Email to reset your password.</p>
            <form action="/forgotpassword" method="post" id="forget-form">
                <input type="email" class="form-control form-input" id="email-forgotpass" name="email" placeholder="Email Address" required>
                <div class="enter-email">
                    <button type="submit" class="btn submit-btn " id="forget-btn">Enter email</button>
                </div>
            </form>
        </div>
    </main>
    <%- include('footer') %>

            <!-------------------------------- password validate ---------------------------------->

            <script>

                function validateEmail(email) {
                    const re = /\S+@\S+\.\S+/;
                    return re.test(email);
                }
                const forgetPasswordForm = document.getElementById('forget-form');
                const emailInput = document.getElementsByName('email')[0];
                const forgetPasswordButton=document.getElementById('forget-btn')
    
                forgetPasswordForm.addEventListener('submit', (e) => {
                    e.preventDefault();
                    if (!validateEmail(emailInput.value)) {
                        Swal.fire({
                            title: "Invalid Email",
                            text: "Please enter a valid email address.",
                            icon: "error"
                        });
                    } else {
                        forgetPasswordForm.submit();
                    }
                });
    
            </script>
