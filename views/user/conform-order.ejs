
    <style>
   
    body {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        background-color: #1a1a1a;
        margin: 0;
        font-family: 'Arial', sans-serif;
        overflow: hidden;
    }

    .success-container {
        text-align: center;
        background-color: #2c2c2c;
        padding: 3em;
        border-radius: 10px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
        transform: scale(0);
        animation: scale-in 0.5s forwards ease-out;
    }

    @keyframes scale-in {
        to {
            transform: scale(1);
        }
    }

    .checkmark-container {
        display: flex;
        justify-content: center;
        margin-bottom: 1em;
    }

    .checkmark {
        width: 100px;
        height: 100px;
        display: block;
    }

    .checkmark__circle {
        stroke: #28a745;
        stroke-width: 4;
        stroke-dasharray: 166;
        stroke-dashoffset: 166;
        stroke-linecap: round;
        animation: draw-circle 1s ease-out forwards;
    }

    .checkmark__check {
        stroke: #28a745;
        stroke-width: 4;
        stroke-dasharray: 48;
        stroke-dashoffset: 48;
        stroke-linecap: round;
        animation: draw-check 1s 0.5s ease-out forwards;
    }

    @keyframes draw-circle {
        to {
            stroke-dashoffset: 0;
        }
    }

    @keyframes draw-check {
        to {
            stroke-dashoffset: 0;
        }
    }

    .main-heading {
        color: #fefeeb;
        font-size: 2em;
        margin-bottom: 0.5em;
    }

    .success-message {
        color: #28a745;
        margin-bottom: 0.5em;
        opacity: 0;
        animation: fade-in 1s 0.5s forwards;
    }

    .order-details {
        color: #dcdcdc;
        margin-bottom: 1em;
        opacity: 0;
        animation: fade-in 1s 1s forwards;
    }

    .additional-info {
        color: #dcdcdc;
        margin-bottom: 1.5em;
        opacity: 0;
        animation: fade-in 1s 1.5s forwards;
    }

    @keyframes fade-in {
        to {
            opacity: 1;
        }
    }

    .button-container {
        display: flex;
        justify-content: center;
        gap: 1em;
        margin-top: 1.5em;
    }

    .btn {
        background-color: #dc3545;
        color: white;
        padding: 0.75em 1.5em;
        text-decoration: none;
        border-radius: 5px;
        transition: background-color 0.3s;
    }

    .btn:hover {
        background-color: #c82333;
    }
</style>


    

<body>
    <div class="success-container">
        <div class="checkmark-container">
            <svg class="checkmark" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 52 52">
                <circle class="checkmark__circle" cx="26" cy="26" r="22" fill="none" />
                <path class="checkmark__check" fill="none" d="M14.1 27.2l7.1 7.2 16.7-16.8" />
            </svg>
        </div>
        <h1 class="main-heading">Thank you!</h1>
        <h2 class="success-message">Your order has been placed successfully.</h2>
        <p class="order-details">We appreciate your purchase. Your order number is <%= orders.order_id %>. You will receive an email confirmation shortly.</p>
        <p class="additional-info">Delivery is expected within 2 - 5 business days.</p>
        <div class="button-container">
            <a href="/orders" class="btn">View My Orders</a>
            <a href="/home" class="btn">Continue Shopping</a>
        </div>
    </div>
</body>

